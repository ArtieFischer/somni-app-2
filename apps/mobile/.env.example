# SECURITY: ElevenLabs Agent IDs should ONLY be stored in backend
# Frontend should never have access to agent IDs
# The backend will handle agent ID mapping based on interpreterId

# Existing environment variables (keep these)
EXPO_PUBLIC_SUPABASE_URL=your_supabase_url
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
EXPO_PUBLIC_SUPABASE_REDIRECT_URL=your_redirect_url
EXPO_PUBLIC_SOMNI_BACKEND_URL=your_backend_url
EXPO_PUBLIC_SOMNI_BACKEND_API_SECRET=your_api_secret

# Dream sharing will use the existing backend URL
# EXPO_PUBLIC_SOMNI_BACKEND_URL=https://somni-backend-production.up.railway.app